services:
  mock:
    build:
      context: .
      dockerfile: mock.Dockerfile
  cbdprotein:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - mock
    ports:
      - 9000:9000
